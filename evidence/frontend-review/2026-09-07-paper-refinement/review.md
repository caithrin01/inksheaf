**Perceptible**

- The reading leaf reads clearly: a warm off-white block behind the serif column, distinctly warmer than the page but far from cream stationery. The serif holds full weight on it; no dulling I can see, and the small caps label stays legible.
- The folio "1" is quiet and correctly placed — it does the "page" work more than the tint does, especially on the phone leaf, where the warm field plus centred folio genuinely suggests a sheet at real width.
- Grain: not resolvable in these downscaled renders on either the sheet or the cover. I can't confirm it's visible, and at a 4.5% cap that's expected; I'd treat it as an at-1× judgement rather than a change.
- Book detail: the hairline sheet edge and thin light band at the block's foot are the strongest paper cue here, and they stay inside the specimen. Side-profile band unchanged in character. No tearing, no wash.

**Restraint**

Right, in my view. The paper language is confined to the two places that earn it — the reading specimen and the object — while the sans column, identity page and captions stay flatly neutral. The added "A suggestion of paper" note is useful and appropriately worded as a limit rather than a licence.

**One correction the pixels justify**

The serif column's text shifted ~5px left between before and after (e.g. "Words worth" starts at ≈772 before, ≈767 after) because the leaf's inset padding now governs the text position rather than the column grid. So the specimen text no longer sits on the same left edge it did, and the leaf's own left border sits outside it. Set the leaf's padding so the serif text keeps its original grid position, with the tint bleeding outward.

**Preference, not defect:** the leaf's four-sided hairline makes it read slightly as a UI card. A tint with no border, or an edge on one or two sides only, would read more like a sheet — but the current version is defensible.
